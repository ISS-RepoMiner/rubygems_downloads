source 'https://rubygems.org'

# gems requiring 3rd party credentials (see config/config_env)
gem 'config_env'
gem 'aws-sdk', '~> 2'

gem 'sinatra'
gem 'thin'

gem 'httparty'
gem 'json'

group :test do
  gem 'minitest'
  gem 'webmock'
  gem 'rack'
  gem 'rack-test'
  gem 'rake'
end

gem 'gems'
